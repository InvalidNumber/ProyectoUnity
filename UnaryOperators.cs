using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnaryOperators : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int number = 5;
        Debug.Log(number++);
        Debug.Log(++number);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
